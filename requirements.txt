# El wizard del cliente es stdlib puro: NO necesita instalar dependencias.
# (Las dependencias de resolución —PySide6, cloudscraper, libtorrent— viven
# del lado del servidor/proveedor y no se distribuyen en esta variante.)

# Solo para desarrollo/pruebas del repositorio:
pytest>=7.0